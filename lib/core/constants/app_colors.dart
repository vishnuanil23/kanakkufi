import 'package:flutter/material.dart';

class AppColors {
  // Background
  static const Color background = Color(0xFFF9F8F6);

  // Primary (modern fintech violet)
  static const Color primary = Color(0xFF7C6EF6);

  // Accent (soft lavender)
  static const Color accent = Color(0xFFBFA2DB);

  // Text
  static const Color text = Color(0xFF2E2E2E);
  static const Color textSecondary = Color(0xFF888888);

  // Status Colors
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF48BF59);
  static const Color warning = Color(0xFFD8A205);

  // Others
  static const Color border = Color(0xFFE0E0E0);
  static const Color white = Colors.white;
}
